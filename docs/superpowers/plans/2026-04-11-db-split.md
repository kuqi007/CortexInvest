# SQLite 双库分离实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `sim_trading.db` 拆为 `config.db`（DELETE 模式，OneDrive 秒级同步）+ `trading.db`（WAL 模式，本地高频写入），解决 Config 变更跨机同步延迟问题。

**Architecture:** config.db 存 4 张配置表（monitor_watchlist/monitor_settings/tag_meta/position_change_log），trading.db 存其余 21 张运行数据表。两个库都在 `src/data/`（OneDrive 同步目录），但 journal mode 和 checkpoint 策略不同。迁移基于文件复制 + DROP TABLE，非逐行 INSERT。

**Tech Stack:** Python 3.13 + SQLite, TypeScript/Next.js + better-sqlite3

**Spec:** `docs/superpowers/specs/2026-04-11-db-split-design.md`

---

## File Structure

| 操作 | 文件 | 职责 |
|---|---|---|
| Modify | `src/sim_trading/db.py` | 拆 SCHEMA、路径常量、连接函数 |
| Modify | `src/utils/config_reader.py` | 改连 config.db |
| Modify | `src/sim_trading/futu_position_sync.py` | 双库写入 |
| Modify | `src/tools/indicator_alert_engine.py` | 移除硬编码路径 |
| Modify | `web/app/lib/db.ts` | 导出双路径 + 双 open 函数 |
| Modify | `web/app/api/config/route.ts` | 改用 openConfigDb |
| Modify | `web/app/api/sector/route.ts` | tag_meta→config, 其余→trading |
| Modify | `web/app/api/close-events/route.ts` | 改用 openTradingDb |
| Modify | `web/app/api/sim/route.ts` | 改用 openTradingDb |
| Modify | `web/app/api/metrics/route.ts` | 改用 openTradingDb |
| Modify | `web/app/api/position-change-log/route.ts` | 改用 openConfigDb |
| Modify | `src/tools/market_data_poller.py` | 添加 trading.db checkpoint |
| Create | `scripts/migrate_split_db.py` | 迁移脚本 |
| Modify | `start_ai_investor_full.sh` | 启动前自动迁移 |

---

### Task 1: Python db.py — 拆分 SCHEMA、路径常量、连接函数

**Files:**
- Modify: `src/sim_trading/db.py`

- [ ] **Step 1: 替换路径常量和 SCHEMA**

将现有的 `DB_PATH`、`SCHEMA`、`get_connection()`、`init_db()` 拆为 config 和 trading 两套。

替换文件顶部的常量（line 6-7）:

```python
# 旧:
DB_PATH = Path(__file__).resolve().parent.parent / "data" / "sim_trading.db"
_db_path_override: str | None = None  # set to ":memory:" in tests

# 新:
DATA_DIR = Path(__file__).resolve().parent.parent / "data"
CONFIG_DB_PATH = DATA_DIR / "config.db"
TRADING_DB_PATH = DATA_DIR / "trading.db"
LEGACY_DB_PATH = DATA_DIR / "sim_trading.db"

_db_path_override: str | None = None  # set to ":memory:" in tests (trading db)
_config_db_path_override: str | None = None  # for tests

# Backward compat
DB_PATH = TRADING_DB_PATH
```

将 `SCHEMA` 拆为 `CONFIG_SCHEMA` 和 `TRADING_SCHEMA`：

```python
CONFIG_SCHEMA = """
-- Monitor 配置（DB 为主，JSON 为快照）
CREATE TABLE IF NOT EXISTS monitor_watchlist (
    symbol TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    list_type TEXT NOT NULL CHECK (list_type IN ('holding', 'watching')),
    cost REAL,
    shares INTEGER,
    lot INTEGER,
    hidden INTEGER NOT NULL DEFAULT 0,
    star INTEGER NOT NULL DEFAULT 0,
    dip_buy INTEGER NOT NULL DEFAULT 0,
    tags TEXT DEFAULT '[]',
    watch_price REAL,
    watch_price_date TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_monitor_watchlist_type ON monitor_watchlist(list_type);
CREATE INDEX IF NOT EXISTS idx_monitor_watchlist_updated ON monitor_watchlist(updated_at);

CREATE TABLE IF NOT EXISTS monitor_settings (
    key TEXT PRIMARY KEY,
    value REAL NOT NULL,
    updated_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS tag_meta (
    tag TEXT PRIMARY KEY,
    star INTEGER DEFAULT 0,
    watch INTEGER DEFAULT 1,
    baseline_value REAL DEFAULT 100,
    parent TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now'))
);

-- 持仓变更记录
CREATE TABLE IF NOT EXISTS position_change_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    ts TEXT NOT NULL,
    source TEXT NOT NULL,
    shares_from INTEGER,
    shares_to INTEGER,
    cost_from REAL,
    cost_to REAL
);
CREATE INDEX IF NOT EXISTS idx_pcl_symbol ON position_change_log(symbol);
CREATE INDEX IF NOT EXISTS idx_pcl_ts ON position_change_log(ts);
CREATE UNIQUE INDEX IF NOT EXISTS idx_pcl_unique
    ON position_change_log(symbol, ts, source, shares_from, shares_to, cost_from, cost_to);
"""
```

`TRADING_SCHEMA` 为现有 `SCHEMA` 中去掉上述 4 张表及其索引后的内容（signals、price_snapshots、trades、daily_pnl、param_versions、live_state、optimization_runs、alert_events、session_snapshots、sector_rotation、sector_daily、stock_daily、sector_alerts、daily_l2_digest、trade_plan_events、futu_orders、daily_kline、market_amo_history）。

保留 `SCHEMA = CONFIG_SCHEMA + TRADING_SCHEMA` 以向后兼容。

- [ ] **Step 2: 添加双库连接函数**

替换 `get_connection()` 和 `init_db()`：

```python
def get_config_connection() -> sqlite3.Connection:
    """Config DB — DELETE mode for OneDrive sync."""
    if _config_db_path_override is not None:
        path = _config_db_path_override
    elif _db_path_override is not None:
        path = _db_path_override  # Fallback for backward compatibility in tests
    else:
        path = str(CONFIG_DB_PATH)
    
    use_uri = path.startswith("file:")
    conn = sqlite3.connect(path, timeout=10, uri=use_uri, isolation_level=None)
    conn.execute("PRAGMA journal_mode=DELETE")
    conn.execute("PRAGMA busy_timeout=5000")
    conn.row_factory = sqlite3.Row
    return conn


def get_connection() -> sqlite3.Connection:
    """Trading DB — WAL mode for performance. (existing API, now points to trading.db)"""
    path = _db_path_override if _db_path_override is not None else str(TRADING_DB_PATH)
    use_uri = path.startswith("file:")
    conn = sqlite3.connect(path, timeout=10, uri=use_uri, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA busy_timeout=5000")
    conn.row_factory = sqlite3.Row
    return conn


def init_config_db():
    """Create config tables if they don't exist."""
    conn = get_config_connection()
    conn.executescript(CONFIG_SCHEMA)
    # Migration: add tags, watch_price, watch_price_date columns to monitor_watchlist
    existing_cols = {
        row[1]
        for row in conn.execute("PRAGMA table_info(monitor_watchlist)").fetchall()
    }
    if "tags" not in existing_cols:
        try:
            conn.execute("ALTER TABLE monitor_watchlist ADD COLUMN tags TEXT DEFAULT '[]'")
        except sqlite3.OperationalError:
            pass
    if "watch_price" not in existing_cols:
        try:
            conn.execute("ALTER TABLE monitor_watchlist ADD COLUMN watch_price REAL")
        except sqlite3.OperationalError:
            pass
    if "watch_price_date" not in existing_cols:
        try:
            conn.execute("ALTER TABLE monitor_watchlist ADD COLUMN watch_price_date TEXT")
        except sqlite3.OperationalError:
            pass
    # Migration: add parent column to tag_meta if missing
    tag_meta_cols = {
        row[1]
        for row in conn.execute("PRAGMA table_info(tag_meta)").fetchall()
    }
    if "parent" not in tag_meta_cols:
        try:
            conn.execute("ALTER TABLE tag_meta ADD COLUMN parent TEXT")
        except sqlite3.OperationalError:
            pass
    conn.commit()
    conn.close()


def init_trading_db():
    """Create all trading tables if they don't exist."""
    conn = get_connection()
    conn.executescript(TRADING_SCHEMA)
    # Migration: add daily_score column if missing (existing DBs)
    try:
        conn.execute("SELECT daily_score FROM live_state LIMIT 1")
    except sqlite3.OperationalError:
        try:
            conn.execute("ALTER TABLE live_state ADD COLUMN daily_score INTEGER DEFAULT 0")
        except sqlite3.OperationalError:
            pass
    # Migration: add name column to live_state if missing
    try:
        conn.execute("SELECT name FROM live_state LIMIT 1")
    except sqlite3.OperationalError:
        try:
            conn.execute("ALTER TABLE live_state ADD COLUMN name TEXT DEFAULT ''")
        except sqlite3.OperationalError:
            pass
    # Migration: add atr_at_entry column to live_state if missing
    try:
        conn.execute("SELECT atr_at_entry FROM live_state LIMIT 1")
    except sqlite3.OperationalError:
        try:
            conn.execute("ALTER TABLE live_state ADD COLUMN atr_at_entry REAL DEFAULT 0")
        except sqlite3.OperationalError:
            pass
    conn.commit()
    conn.close()


def init_db():
    """Backward compatible init for existing tests. Initializes both."""
    init_config_db()
    init_trading_db()

def init_all_dbs():
    """Explicitly initialize both databases (used in main)."""
    init_config_db()
    init_trading_db()
```

更新 `__main__` 块：

```python
if __name__ == "__main__":
    init_all_dbs()
    for label, path in [("Config", CONFIG_DB_PATH), ("Trading", TRADING_DB_PATH)]:
        print(f"\n{label} DB at {path}")
        conn = sqlite3.connect(str(path))
        conn.row_factory = sqlite3.Row
        tables = conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
        for t in tables:
            count = conn.execute(f"SELECT COUNT(*) FROM {t['name']}").fetchone()[0]
            print(f"  {t['name']}: {count} rows")
        conn.close()
```

- [ ] **Step 3: 验证**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor && uv run python src/sim_trading/db.py`

Expected: 创建 config.db 和 trading.db，输出各自表和行数。

- [ ] **Step 4: 运行现有测试确认无回归**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor && uv run pytest src/sim_trading/test_sim_trading.py -q`

Expected: 所有测试通过（测试用 `_db_path_override = ":memory:"`，不依赖文件路径）。

- [ ] **Step 5: Commit**

```bash
git add src/sim_trading/db.py
git commit -m "refactor: split db.py into config.db + trading.db schemas and connections"
```

---

### Task 2: TypeScript db.ts — 双路径 + 双 open 函数

**Files:**
- Modify: `web/app/lib/db.ts`

- [ ] **Step 1: 替换 db.ts**

当前内容（line 1-10）：
```typescript
import { join } from "path";
export const SIM_DB_PATH = join(process.cwd(), "..", "src", "data", "sim_trading.db");
```

替换为：
```typescript
import { join } from "path";
import Database from "better-sqlite3";

const DATA_DIR = join(process.cwd(), "..", "src", "data");

export const CONFIG_DB_PATH = join(DATA_DIR, "config.db");
export const TRADING_DB_PATH = join(DATA_DIR, "trading.db");

// Backward compat — remove after all routes migrated
export const SIM_DB_PATH = TRADING_DB_PATH;

export function openConfigDb(readonly = false) {
  const db = new Database(CONFIG_DB_PATH, { readonly });
  if (!readonly) db.pragma("journal_mode = DELETE");
  db.pragma("busy_timeout = 15000");
  return db;
}

export function openTradingDb(readonly = false) {
  const db = new Database(TRADING_DB_PATH, { readonly });
  if (!readonly) db.pragma("journal_mode = WAL");
  db.pragma("busy_timeout = 15000");
  return db;
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor/web && npx tsc --noEmit`

Expected: 无类型错误。

- [ ] **Step 3: Commit**

```bash
git add web/app/lib/db.ts
git commit -m "refactor: add dual DB paths and open functions to db.ts"
```

---

### Task 3: Web API routes — config route 迁移

**Files:**
- Modify: `web/app/api/config/route.ts`

- [ ] **Step 1: 替换 DB 路径导入**

当前（line 8）：
```typescript
const SIM_DB_PATH = join(process.cwd(), "..", "src", "data", "sim_trading.db");
```

替换为：
```typescript
import { openConfigDb, CONFIG_DB_PATH } from "../../lib/db";
```

需要额外在文件顶部添加 `import Database from "better-sqlite3";`（如果还没有的话）。

- [ ] **Step 2: 替换 openMonitorDb 函数**

当前（line 140-148）：
```typescript
function openMonitorDb(readonly = false): MonitorDb {
  if (readonly) {
    return new Database(SIM_DB_PATH, { readonly: true });
  }
  const db = new Database(SIM_DB_PATH);
  db.pragma("journal_mode = WAL");
  db.pragma("busy_timeout = 15000");
  return db;
}
```

替换为：
```typescript
function openMonitorDb(readonly = false): MonitorDb {
  return openConfigDb(readonly);
}
```

- [ ] **Step 3: 移除 checkpointForSync 函数**

DELETE 模式不需要 checkpoint。删除：
```typescript
/** Flush WAL to main DB file so OneDrive can sync changes. */
function checkpointForSync(db: MonitorDb) {
  try { db.pragma("wal_checkpoint(TRUNCATE)"); } catch { /* best-effort */ }
}
```

以及 `exportMonitorSnapshotFromDb` 中的两处 `checkpointForSync(db)` 调用。

- [ ] **Step 4: 验证 Web 编译**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor/web && npx tsc --noEmit`

Expected: 无错误。

- [ ] **Step 5: Commit**

```bash
git add web/app/api/config/route.ts
git commit -m "refactor: config route uses openConfigDb (DELETE mode)"
```

---

### Task 4: Web API routes — position-change-log 迁移

**Files:**
- Modify: `web/app/api/position-change-log/route.ts`

- [ ] **Step 1: 替换 DB 路径**

当前（line 5）：
```typescript
const SIM_DB_PATH = join(process.cwd(), "..", "src", "data", "sim_trading.db");
```

替换为：
```typescript
import { openConfigDb } from "../../lib/db";
```

- [ ] **Step 2: 替换 Database 构造**

当前（line 16）：
```typescript
const db = new Database(SIM_DB_PATH, { readonly: true });
```

替换为：
```typescript
const db = openConfigDb(true);
```

- [ ] **Step 3: 移除顶部不再需要的 `import Database from "better-sqlite3";` 和 `import { join } from "path";`**

- [ ] **Step 4: 验证编译**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor/web && npx tsc --noEmit`

- [ ] **Step 5: Commit**

```bash
git add web/app/api/position-change-log/route.ts
git commit -m "refactor: position-change-log uses openConfigDb"
```

---

### Task 5: Web API routes — close-events, sim, metrics 迁移

**Files:**
- Modify: `web/app/api/close-events/route.ts`
- Modify: `web/app/api/sim/route.ts`
- Modify: `web/app/api/metrics/route.ts`

这三个 route 都只访问 trading 表（alert_events, live_state, trades, indicator_cache 等）。

- [ ] **Step 1: close-events/route.ts**

当前（line 5, 15）：
```typescript
const SIM_DB_PATH = join(process.cwd(), "..", "src", "data", "sim_trading.db");
...
const db = new Database(SIM_DB_PATH, { readonly: true });
```

替换为：
```typescript
import { openTradingDb } from "../../lib/db";
...
const db = openTradingDb(true);
```

移除 `import Database from "better-sqlite3";` 和 `import { join } from "path";`。

- [ ] **Step 2: sim/route.ts**

当前（line 6）：
```typescript
import { SIM_DB_PATH } from "../../lib/db";
```

替换为：
```typescript
import { openTradingDb } from "../../lib/db";
```

将所有 `new Database(SIM_DB_PATH, { readonly: true })`（line 194, 388）替换为 `openTradingDb(true)`。

- [ ] **Step 3: metrics/route.ts**

当前（line 5）：
```typescript
import { SIM_DB_PATH } from "../../lib/db";
```

替换为：
```typescript
import { openTradingDb } from "../../lib/db";
```

将所有 `new Database(SIM_DB_PATH, { readonly: true })`（line 68, 220）替换为 `openTradingDb(true)`。

- [ ] **Step 4: 验证编译**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor/web && npx tsc --noEmit`

- [ ] **Step 5: Commit**

```bash
git add web/app/api/close-events/route.ts web/app/api/sim/route.ts web/app/api/metrics/route.ts
git commit -m "refactor: close-events, sim, metrics use openTradingDb"
```

---

### Task 6: Web API routes — sector route 迁移（跨库）

**Files:**
- Modify: `web/app/api/sector/route.ts`

sector route 较复杂：tag_meta 和 monitor_watchlist 操作需要 config.db，sector_daily/rotation/alerts 操作需要 trading.db。

- [ ] **Step 1: 替换 DB 导入**

当前（line 6）：
```typescript
import { SIM_DB_PATH } from "../../lib/db";
```

替换为：
```typescript
import { openConfigDb, openTradingDb } from "../../lib/db";
```

- [ ] **Step 2: 替换所有 `new Database(SIM_DB_PATH...)` 调用**

按操作类型替换：

| 操作 | 当前 | 替换为 |
|---|---|---|
| GET（读取 tag_meta, monitor_watchlist, sector_daily） | `new Database(SIM_DB_PATH, { readonly: true })` | `openConfigDb(true)` for tag reads + `openTradingDb(true)` for sector reads |
| POST sector_rotation 写入（line ~278） | `new Database(SIM_DB_PATH)` | `openTradingDb()` |
| POST tag CRUD（tag_meta + monitor_watchlist，line ~755-975） | `new Database(SIM_DB_PATH)` | `openConfigDb()` |
| DELETE tag（同时清理 sector_daily/sector_alerts，line ~845-902） | `new Database(SIM_DB_PATH)` | 需要两个连接：`openConfigDb()` for tag_meta/monitor_watchlist, `openTradingDb()` for sector_daily/sector_alerts |

对于 DELETE tag 这类跨库操作，需要开两个 db 连接。代码模式：

```typescript
// 跨库操作示例
const configDb = openConfigDb();
const tradingDb = openTradingDb();
try {
  // 从 config.db 删除 tag_meta
  configDb.prepare("DELETE FROM tag_meta WHERE tag = ?").run(tagName);
  // 从 config.db 更新 monitor_watchlist tags
  // ...
  // 从 trading.db 删除关联数据
  tradingDb.prepare("DELETE FROM sector_daily WHERE index_id = ?").run(tagName);
  tradingDb.prepare("DELETE FROM sector_alerts WHERE index_id = ?").run(tagName);
} finally {
  configDb.close();
  tradingDb.close();
}
```

- [ ] **Step 3: 验证编译**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor/web && npx tsc --noEmit`

- [ ] **Step 4: Commit**

```bash
git add web/app/api/sector/route.ts
git commit -m "refactor: sector route uses openConfigDb for tags, openTradingDb for sector data"
```

---

### Task 7: Python config_reader + indicator_alert_engine 迁移

**Files:**
- Modify: `src/utils/config_reader.py`
- Modify: `src/tools/indicator_alert_engine.py`

- [ ] **Step 1: config_reader.py 改连 config.db**

当前（line 23）：
```python
DB_PATH = PROJECT_ROOT / "src" / "data" / "sim_trading.db"
```

替换为：
```python
from src.sim_trading.db import CONFIG_DB_PATH

DB_PATH = CONFIG_DB_PATH
```

- [ ] **Step 2: indicator_alert_engine.py 移除硬编码路径**

找到硬编码的 `DB_PATH` 常量（约 line 184），替换为：
```python
from src.sim_trading.db import get_connection
```

将所有 `sqlite3.connect(str(DB_PATH))` 替换为 `get_connection()`，删除本地的 `DB_PATH` 定义。indicator_cache 表在 trading.db 中。

- [ ] **Step 3: 验证 Python 导入无误**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor && uv run python -c "from src.utils.config_reader import read_monitor_config; from src.sim_trading.db import get_config_connection, get_connection; print('OK')"`

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add src/utils/config_reader.py src/tools/indicator_alert_engine.py
git commit -m "refactor: config_reader uses config.db, indicator_engine uses trading.db"
```

---

### Task 8: Python futu_position_sync — 跨库写入

**Files:**
- Modify: `src/sim_trading/futu_position_sync.py`

- [ ] **Step 1: 添加 config 连接导入**

在文件顶部导入区添加：
```python
from src.sim_trading.db import get_config_connection
```

- [ ] **Step 2: 修改 position_change_log 写入**

找到所有 `INSERT INTO position_change_log` 的位置（约 line 138），将连接从 `get_connection()` 改为 `get_config_connection()`。

具体做法：在该函数中，开两个连接：
```python
trading_conn = get_connection()
config_conn = get_config_connection()
try:
    # trades, daily_pnl, futu_orders 写 trading_conn
    # position_change_log 写 config_conn
    ...
finally:
    trading_conn.close()
    config_conn.close()
```

对于部分失败，在 except 块中用 `logging.warning` 记录但不阻止主流程。

- [ ] **Step 3: 运行相关测试**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor && uv run pytest src/sim_trading/test_sim_trading.py -q`

Expected: 全部通过。

- [ ] **Step 4: Commit**

```bash
git add src/sim_trading/futu_position_sync.py
git commit -m "refactor: futu_position_sync writes to dual databases"
```

---

### Task 9: Poller checkpoint + 其他 Python 文件确认

**Files:**
- Modify: `src/tools/market_data_poller.py`
- Modify: `src/tools/screenshot_stock_import.py` (如有必要)
- Modify: `src/tools/monitor_config_db_migrator.py` (如有必要)

- [ ] **Step 1: market_data_poller.py 添加 trading.db checkpoint**

在 poller 主循环的末尾（每轮循环完成后），添加：
```python
conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
```

找到 poller 主循环的位置，确认在适当位置添加 checkpoint。

- [ ] **Step 2: screenshot_stock_import.py 改连 config.db**

检查文件中的 DB 连接方式，如果直接用 `sqlite3.connect(str(DB_PATH))` 则改为 `get_config_connection()`。

- [ ] **Step 3: monitor_config_db_migrator.py 改连 config.db**

检查并修改连接方式。

- [ ] **Step 4: 运行全量测试**

Run: `cd /Users/zhul1/Documents/aiWorkspace/ai-investor && uv run pytest src/sim_trading/test_sim_trading.py -v`

Expected: 138 tests passed。

- [ ] **Step 5: Commit**

```bash
git add src/tools/market_data_poller.py src/tools/screenshot_stock_import.py src/tools/monitor_config_db_migrator.py
git commit -m "refactor: poller adds trading.db checkpoint, import/migrator use config.db"
```

---

### Task 10: 迁移脚本

**Files:**
- Create: `scripts/migrate_split_db.py`

- [ ] **Step 1: 编写迁移脚本**

```python
#!/usr/bin/env python3
"""Migrate sim_trading.db → config.db + trading.db (file-copy based).

Idempotent: skips if config.db and trading.db already exist and sim_trading.db is absent.
"""

import shutil
import sqlite3
import sys
from pathlib import Path

DATA_DIR = Path(__file__).resolve().parent.parent / "src" / "data"
LEGACY_DB = DATA_DIR / "sim_trading.db"
CONFIG_DB = DATA_DIR / "config.db"
TRADING_DB = DATA_DIR / "trading.db"
BACKUP_DB = DATA_DIR / "sim_trading.db.pre-split.bak"

CONFIG_TABLES = {"monitor_watchlist", "monitor_settings", "tag_meta", "position_change_log"}


def get_tables(db_path: Path) -> set[str]:
    conn = sqlite3.connect(str(db_path))
    rows = conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'").fetchall()
    conn.close()
    return {r[0] for r in rows}


def get_row_counts(db_path: Path) -> dict[str, int]:
    conn = sqlite3.connect(str(db_path))
    tables = get_tables(db_path)
    counts = {}
    for t in tables:
        counts[t] = conn.execute(f"SELECT COUNT(*) FROM [{t}]").fetchone()[0]
    conn.close()
    return counts


def main():
    # Already migrated?
    if CONFIG_DB.exists() and TRADING_DB.exists() and not LEGACY_DB.exists():
        print("Already migrated. Nothing to do.")
        return

    if not LEGACY_DB.exists():
        print("No legacy sim_trading.db found. Fresh install.")
        return

    print(f"Migrating {LEGACY_DB} ({LEGACY_DB.stat().st_size / 1024 / 1024:.1f} MB)...")

    # Step 1: Checkpoint WAL
    conn = sqlite3.connect(str(LEGACY_DB))
    try:
        conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
    except Exception:
        pass
    conn.close()

    # Step 2: Verify legacy tables
    legacy_tables = get_tables(LEGACY_DB)
    print(f"  Legacy tables: {len(legacy_tables)}")
    legacy_counts = get_row_counts(LEGACY_DB)

    # Step 3: Copy → config.db, drop trading tables, VACUUM
    print("  Creating config.db...")
    shutil.copy2(str(LEGACY_DB), str(CONFIG_DB))
    conn = sqlite3.connect(str(CONFIG_DB))
    conn.execute("PRAGMA journal_mode=DELETE")
    trading_tables = legacy_tables - CONFIG_TABLES
    for t in trading_tables:
        if t in legacy_tables:
            conn.execute(f"DROP TABLE IF EXISTS [{t}]")
    # Drop indexes that belong to trading tables
    for idx_row in conn.execute("SELECT name, tbl_name FROM sqlite_master WHERE type='index' AND name NOT LIKE 'sqlite_%'").fetchall():
        if idx_row[1] not in CONFIG_TABLES:
            conn.execute(f"DROP INDEX IF EXISTS [{idx_row[0]}]")
    conn.execute("VACUUM")
    conn.close()
    print(f"  config.db: {CONFIG_DB.stat().st_size / 1024:.0f} KB")

    # Step 4: Copy → trading.db, drop config tables
    print("  Creating trading.db...")
    shutil.copy2(str(LEGACY_DB), str(TRADING_DB))
    conn = sqlite3.connect(str(TRADING_DB))
    conn.execute("PRAGMA journal_mode=WAL")
    for t in CONFIG_TABLES:
        if t in legacy_tables:
            conn.execute(f"DROP TABLE IF EXISTS [{t}]")
    for idx_row in conn.execute("SELECT name, tbl_name FROM sqlite_master WHERE type='index' AND name NOT LIKE 'sqlite_%'").fetchall():
        if idx_row[1] in CONFIG_TABLES:
            conn.execute(f"DROP INDEX IF EXISTS [{idx_row[0]}]")
    conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
    conn.close()

    # Step 5: Verify
    config_counts = get_row_counts(CONFIG_DB)
    trading_counts = get_row_counts(TRADING_DB)
    print("\n  Verification:")
    all_ok = True
    for t, expected in legacy_counts.items():
        actual = config_counts.get(t, trading_counts.get(t, 0))
        status = "OK" if actual == expected else "MISMATCH"
        if status != "OK":
            all_ok = False
        print(f"    {t}: {expected} → {actual} [{status}]")

    if not all_ok:
        print("\n  WARNING: Row count mismatch detected!")
        print("  Keeping legacy DB. Manual investigation required.")
        CONFIG_DB.unlink(missing_ok=True)
        TRADING_DB.unlink(missing_ok=True)
        sys.exit(1)

    # Step 6: Backup legacy
    shutil.move(str(LEGACY_DB), str(BACKUP_DB))
    print(f"\n  Legacy backed up to: {BACKUP_DB}")
    print("  Migration complete!")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 测试迁移脚本（dry run）**

先备份当前 DB，然后运行：

```bash
cp src/data/sim_trading.db src/data/sim_trading.db.test-backup
uv run python scripts/migrate_split_db.py
```

Expected:
- 创建 config.db (~24KB) 和 trading.db (~16MB)
- 行数验证全 OK
- sim_trading.db 被重命名为 .pre-split.bak

- [ ] **Step 3: 验证迁移结果**

```bash
uv run python -c "
import sqlite3
for name, path in [('Config', 'src/data/config.db'), ('Trading', 'src/data/trading.db')]:
    conn = sqlite3.connect(path)
    tables = conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()
    print(f'{name}: {[t[0] for t in tables]}')
    conn.close()
"
```

Expected:
- Config: `['monitor_watchlist', 'monitor_settings', 'tag_meta', 'position_change_log']`
- Trading: 无 config 表，其余全在

- [ ] **Step 4: 恢复 + 清理测试文件**

```bash
mv src/data/sim_trading.db.pre-split.bak src/data/sim_trading.db
rm src/data/config.db src/data/trading.db
rm src/data/sim_trading.db.test-backup
```

- [ ] **Step 5: Commit**

```bash
git add scripts/migrate_split_db.py
git commit -m "feat: add db split migration script (file-copy based)"
```

---

### Task 11: 启动脚本 + 全量验证

**Files:**
- Modify: `start_ai_investor_full.sh`

- [ ] **Step 1: 在启动脚本中添加迁移步骤**

在 `start_ai_investor_full.sh` 中，在启动 Python/Web 服务之前添加：

```bash
# DB split migration (idempotent, skips if already done)
echo "Checking DB migration..."
uv run python scripts/migrate_split_db.py
```

- [ ] **Step 2: 运行迁移脚本 + 全量 Python 测试**

```bash
uv run python scripts/migrate_split_db.py
uv run pytest src/sim_trading/test_sim_trading.py -q
```

Expected: 迁移成功 + 138 tests passed。

- [ ] **Step 3: Web 编译验证**

```bash
cd web && npx tsc --noEmit
```

Expected: 无错误。

- [ ] **Step 4: Commit**

```bash
git add start_ai_investor_full.sh
git commit -m "feat: auto-run db split migration on startup"
```

---

### Task 12: 冒烟测试 + 最终验证

**Files:**
- 无新文件

- [ ] **Step 1: 冒烟测试**

```bash
uv run pytest src/sim_trading/test_sim_trading.py -m smoke -q
```

Expected: 14 tests passed。

- [ ] **Step 2: Web 启动测试**

```bash
cd web && npm run build
```

Expected: 构建成功。

- [ ] **Step 3: 手动验证（说明）**

说明用户需手动验证：
1. 启动服务：`./start_ai_investor_full.sh`
2. 在 Web 上更新一个标签
3. 检查另一台机器是否立即可见（config.db DELETE 模式，秒级同步）
4. 检查 `src/data/config.db` 大小应远小于 `trading.db`

- [ ] **Step 4: Final commit（如有 lint/格式修正）**

```bash
git add -A
git commit -m "chore: db split complete — config.db + trading.db"
```

---

## Self-Review

**1. Spec coverage:**
- config.db 4 张表 (monitor_watchlist, monitor_settings, tag_meta, position_change_log) → Task 1, 3, 4, 7
- trading.db 21 张表 → Task 1, 5, 6, 9
- DELETE/WAL journal mode → Task 1, 2
- Checkpoint 策略 → Task 9
- 迁移脚本（文件复制 + DROP + VACUUM） → Task 10
- 启动脚本改动 → Task 11
- 回退方案（.pre-split.bak） → Task 10

**2. Placeholder scan:** 无 TBD/TODO。所有步骤有具体代码。

**3. Type consistency:**
- Python: `get_config_connection()` 在 Task 1 定义，Task 7/8 使用 ✓
- Python: `_config_db_path_override` 在 Task 1 定义 ✓
- TypeScript: `openConfigDb()`/`openTradingDb()` 在 Task 2 定义，Task 3-6 使用 ✓
- `CONFIG_DB_PATH`/`TRADING_DB_PATH` 命名一致 ✓
